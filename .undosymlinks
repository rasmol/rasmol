#!/bin/csh
foreach file ( index.shtml RasMol.tar.gz RasMol.DEC RasMol.HP \
RasMol.LINUX RasMol.MAC RasMol.MSWIN RasMol.RS6K RasMol.SGI )
  rm -rf $file
end
foreach file (*)
  if ( -d $file ) then
    if ( -e $file/.undosymlinks ) then
      (cd $file; source .undosymlinks)
    endif
  endif
end

